export interface ActivityModel {
    ID: number,
    Name: string,
    Route: string, // Check back which type will the route be
    Date: string,
    Duration: number, 
    Kilometers: number,
    Type: string,
    ChallengeID: number
}
